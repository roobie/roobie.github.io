---
phase: 04-apply-edits-as-per-the-frontend-designer-s-suggestions
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All visual changes render correctly in both light and dark mode"
    - "Serif body font is visible on blog post prose, monospace on code and UI chrome"
    - "Tag pills on cards are readable and link to correct tag pages"
    - "Dark mode borders are visually distinguishable"
    - "Hero section reads naturally with personality"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all frontend design changes from Plans 01 and 02.

Purpose: Confirm that typography, hero, card improvements, dark mode contrast, footer license, and about page all render correctly before merging.
Output: Human approval of visual design changes.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-apply-edits-as-per-the-frontend-designer-s-suggestions/04-01-SUMMARY.md
@.planning/phases/04-apply-edits-as-per-the-frontend-designer-s-suggestions/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run final build check</name>
  <files></files>
  <action>
Run `cd /home/jani/devel/roobie.github.io/site && pnpm run build` to confirm the full build passes.
Then run `cd /home/jani/devel/roobie.github.io && just dev` in the background to start the dev server for visual inspection.
  </action>
  <verify>
    <automated>cd /home/jani/devel/roobie.github.io/site && pnpm run build</automated>
  </verify>
  <done>Build passes, dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all design changes</name>
  <action>
Human visually inspects all design changes in the browser. Dev server should be running at http://localhost:4321/.

What was built: All frontend design improvements from the design review -- proportional body font (Source Serif 4), homepage hero rewrite, post card tag pills and description hierarchy, dark mode border contrast fix, footer CC BY 4.0 license, about page bio, date format fix (no comma), and pagination config bump.

Verification steps:
1. Visit http://localhost:4321/ -- verify:
   - Hero has a personality-driven tagline (no "Welcome to my personal blog")
   - No README link, no "Social Links:" label
   - 6 recent posts shown (was 4)
   - Post cards show smaller/muted description text
   - Post cards show tag pills (small, rounded) below descriptions
   - Date format has no comma (e.g. "16 Mar 2026")
2. Click a blog post -- verify:
   - Prose body text renders in a serif font (Source Serif 4)
   - Code blocks render in monospace (Google Sans Code)
   - Navigation/header stays monospace
3. Toggle dark mode (theme toggle in header) -- verify:
   - Section borders are visible against the dark background (not washed out)
   - Tag pills are readable in dark mode
4. Visit http://localhost:4321/about/ -- verify:
   - Real bio present (3-5 sentences about the author)
   - CC BY 4.0 license link present
5. Scroll to footer on any page -- verify:
   - Shows "CC BY 4.0" with a link
   - Does NOT say "All rights reserved"
6. Visit http://localhost:4321/posts/ -- verify:
   - 8 posts per page (was 4)
  </action>
  <verify>Human approval</verify>
  <done>Human approves all visual changes or describes issues to fix.</done>
</task>

</tasks>

<verification>
Human confirms all visual changes render correctly in both light and dark mode.
</verification>

<success_criteria>
- Human approves all visual changes
- No regressions identified
</success_criteria>

<output>
After completion, create `.planning/phases/04-apply-edits-as-per-the-frontend-designer-s-suggestions/04-03-SUMMARY.md`
</output>
