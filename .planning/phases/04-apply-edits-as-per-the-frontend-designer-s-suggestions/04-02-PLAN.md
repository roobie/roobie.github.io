---
phase: 04-apply-edits-as-per-the-frontend-designer-s-suggestions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/pages/index.astro
  - site/src/components/Card.astro
  - site/src/components/Footer.astro
  - site/src/pages/about.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Homepage hero has a personality-driven tagline, no boilerplate 'Welcome to my personal blog' text"
    - "Homepage has no README link and no 'Social Links:' label"
    - "Post cards show description in smaller/lower-contrast text below the date"
    - "Post cards show first 2-3 tag pills below the description, linking to correct tag pages"
    - "Footer says 'CC BY 4.0' with a link, not 'All rights reserved'"
    - "About page has a real bio (3-5 sentences) about the author"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Rewritten hero section"
      min_lines: 30
    - path: "site/src/components/Card.astro"
      provides: "Description hierarchy and tag pills"
      contains: "tags"
    - path: "site/src/components/Footer.astro"
      provides: "CC BY 4.0 license link"
      contains: "CC BY 4.0"
    - path: "site/src/pages/about.md"
      provides: "Author bio"
      min_lines: 8
  key_links:
    - from: "site/src/components/Card.astro"
      to: "/tags/{slug}/"
      via: "Tag pill anchor hrefs"
      pattern: "slugifyStr"
    - from: "site/src/components/Footer.astro"
      to: "creativecommons.org"
      via: "License link href"
      pattern: "creativecommons.org/licenses/by/4.0"
---

<objective>
Rewrite homepage hero, improve post card visual hierarchy with tag pills, fix footer license, and write about page bio.

Purpose: Apply the content and component-level design improvements from the frontend review -- hero personality, card information hierarchy, license consistency, and a real author bio.
Output: Updated index hero, Card component with description styling and tag pills, Footer with CC BY 4.0, About page with bio.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-apply-edits-as-per-the-frontend-designer-s-suggestions/04-RESEARCH.md

<interfaces>
<!-- Current Card.astro structure -->
From site/src/components/Card.astro:
```astro
---
import type { CollectionEntry } from "astro:content";
import { slugifyStr } from "@/utils/slugify";
import { getPath } from "@/utils/getPath";
import Datetime from "./Datetime.astro";

type Props = {
  variant?: "h2" | "h3";
} & CollectionEntry<"blog">;

const { variant: Heading = "h2", id, data, filePath } = Astro.props;
const { title, description, ...props } = data;
---
<li class="my-6">
  <a href={getPath(id, filePath)} class:list={[...]}>
    <Heading style={{ viewTransitionName: slugifyStr(title.replaceAll(".", "-")) }}>
      {title}
    </Heading>
  </a>
  <Datetime {...props} />
  <p>{description}</p>
</li>
```

<!-- Current index.astro hero section (lines 25-66) -->
From site/src/pages/index.astro:
```astro
<section id="hero" class:list={["pt-8 pb-6", "border-b border-border"]}>
  <h1 class="my-4 inline-block text-4xl font-bold sm:my-8 sm:text-5xl">
    Bjorn's cogitations
  </h1>
  <!-- RSS icon -->
  <p>Welcome to my personal blog where I share thoughts on philosophy, technology, and life...</p>
  <p class="mt-2">Read the blog posts or check <LinkButton href="...README">README</LinkButton>...</p>
  {SOCIALS.length > 0 && (
    <div class="mt-4 flex max-sm:flex-col sm:items-center">
      <div class="me-2 mb-1 whitespace-nowrap sm:mb-0">Social Links:</div>
      <Socials />
    </div>
  )}
</section>
```

<!-- Current Footer.astro (lines 27-31) -->
From site/src/components/Footer.astro:
```astro
<div class="my-2 flex flex-col items-center whitespace-nowrap sm:flex-row">
  <span>Copyright &#169; {currentYear}</span>
  <span class="hidden sm:inline">&nbsp;|&nbsp;</span>
  <span>All rights reserved.</span>
</div>
```

<!-- Current about.md -->
From site/src/pages/about.md:
```markdown
---
layout: ../layouts/AboutLayout.astro
title: "About"
---
Content in this blog is licensed by CC BY 4.0 unless otherwise indicated.
This blog is powered by AstroPaper
AstroPaper is a minimal, accessible and SEO-friendly blog theme...
```

<!-- slugifyStr is already imported in Card.astro -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite homepage hero and fix footer license</name>
  <files>site/src/pages/index.astro, site/src/components/Footer.astro</files>
  <action>
1. In `site/src/pages/index.astro`, replace the hero section content (lines 25-66). Keep the `<section id="hero">`, the `<h1>`, and the RSS icon/link exactly as they are. Remove:
   - The first `<p>` with "Welcome to my personal blog..." boilerplate
   - The second `<p>` with the README link (and the `LinkButton` import if unused elsewhere -- check first; it IS used for "All Posts" button below, so keep the import)
   - The `<div class="me-2 mb-1 whitespace-nowrap sm:mb-0">Social Links:</div>` label

   Replace with a single personality-driven `<p>` tagline. Claude's discretion on exact wording -- it should reflect philosophy + technology + life themes. Suggested starting point:
   ```
   <p class="mt-4">
     Thoughts on building software, reasoning about systems, and the occasional
     philosophical detour. Written to think out loud.
   </p>
   ```

   Keep the Socials block but remove the "Social Links:" label div. The socials div should just be:
   ```
   {SOCIALS.length > 0 && (
     <div class="mt-4">
       <Socials />
     </div>
   )}
   ```

2. In `site/src/components/Footer.astro`, replace the "All rights reserved." span (line 30-31) with a CC BY 4.0 link:
   ```astro
   <span>
     Content licensed under{" "}
     <a
       href="https://creativecommons.org/licenses/by/4.0/"
       class="underline decoration-dashed underline-offset-4 hover:text-accent"
       target="_blank"
       rel="noopener noreferrer"
     >
       CC BY 4.0
     </a>
   </span>
   ```
   Remove the "All rights reserved." text entirely.
  </action>
  <verify>
    <automated>cd /home/jani/devel/roobie.github.io/site && pnpm run build</automated>
  </verify>
  <done>Build succeeds. Hero has new tagline, no README link, no "Social Links:" label. Footer shows "CC BY 4.0" link, no "All rights reserved" text.</done>
</task>

<task type="auto">
  <name>Task 2: Add card description hierarchy and tag pills, update about page</name>
  <files>site/src/components/Card.astro, site/src/pages/about.md</files>
  <action>
1. In `site/src/components/Card.astro`:

   a. Update the destructuring on line 13 to include `tags`:
      ```
      const { title, description, tags, ...props } = data;
      ```

   b. Add `text-sm opacity-70` classes to the description `<p>` element (line 32) to reduce its visual weight:
      ```
      <p class="text-sm opacity-70">{description}</p>
      ```

   c. After the description `<p>`, add inline tag pills showing up to 3 tags. Use `slugifyStr` (already imported) for the tag URL slugs:
      ```astro
      {tags && tags.length > 0 && (
        <ul class="mt-1 flex flex-wrap gap-1">
          {tags.slice(0, 3).map(tag => (
            <li>
              <a
                href={`/tags/${slugifyStr(tag)}/`}
                class="inline-block rounded border border-border px-1.5 py-0.5 text-xs text-foreground/70 hover:border-accent hover:text-accent"
              >
                {tag}
              </a>
            </li>
          ))}
        </ul>
      )}
      ```

   The guard `{tags && tags.length > 0 && (...)}` prevents TypeScript errors since `tags` is optional (`string[] | undefined`).

2. In `site/src/pages/about.md`, replace the entire content (keep frontmatter) with a real bio. Claude's discretion on exact wording -- should be 3-5 sentences about what the author works on, thinks about, and why they write. Suggested starting point:

   ```markdown
   ---
   layout: ../layouts/AboutLayout.astro
   title: "About"
   ---

   I'm Bjorn Roberg, a software developer based in Sweden. I spend my days building
   systems that have to work reliably, and my spare time wondering why they don't.
   I write here to slow down my thinking -- to examine ideas about technology,
   epistemology, and how we reason about complex things. If any of it is useful to
   you, great; if it starts a conversation, even better.

   Content in this blog is licensed under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)
   unless otherwise indicated.

   This blog is powered by [AstroPaper](https://github.com/satnaing/astro-paper).
   ```

   Keep the CC BY 4.0 line and AstroPaper credit. Note: use the proper name with the umlaut "Bjorn" as it appears in the existing config (`Bjorn Roberg`).
  </action>
  <verify>
    <automated>cd /home/jani/devel/roobie.github.io/site && pnpm run build</automated>
  </verify>
  <done>Build succeeds. Card descriptions have reduced visual weight (text-sm opacity-70). Tag pills appear on cards (up to 3 tags, linked to tag pages). About page has a real bio.</done>
</task>

</tasks>

<verification>
- `cd site && pnpm run build` exits 0
- `grep -c "Welcome to my personal blog" site/src/pages/index.astro` returns 0
- `grep -c "README" site/src/pages/index.astro` returns 0
- `grep -c "Social Links:" site/src/pages/index.astro` returns 0
- `grep "CC BY 4.0" site/src/components/Footer.astro` matches
- `grep -c "All rights reserved" site/src/components/Footer.astro` returns 0
- `grep "tags" site/src/components/Card.astro` matches (destructured and rendered)
- `grep "text-sm opacity-70" site/src/components/Card.astro` matches
- `grep "slugifyStr(tag)" site/src/components/Card.astro` matches
</verification>

<success_criteria>
- Build passes with no TypeScript errors
- Homepage hero is personality-driven with no boilerplate, no README link, no "Social Links:" label
- Card descriptions are visually de-emphasized
- Tag pills render on cards and link to correct tag pages
- Footer shows CC BY 4.0 link consistently with About page
- About page has a genuine bio
</success_criteria>

<output>
After completion, create `.planning/phases/04-apply-edits-as-per-the-frontend-designer-s-suggestions/04-02-SUMMARY.md`
</output>
