---
phase: 08-structured-data-e-e-a-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/layouts/Layout.astro
  - site/src/pages/about.md
autonomous: true
requirements:
  - Ad-hoc-1
  - Ad-hoc-2

must_haves:
  truths:
    - "JSON-LD BlogPosting schema includes publisher field with type Person, name, and URL"
    - "JSON-LD BlogPosting schema includes mainEntityOfPage field with type WebPage and canonical URL as @id"
    - "About page lists expertise areas: AI agents, developer tooling, software architecture, Rust/TypeScript/Go"
    - "About page links to casq, cairn, and slog blog posts"
    - "About page retains CC BY 4.0 license notice and AstroPaper attribution"
  artifacts:
    - path: "site/src/layouts/Layout.astro"
      provides: "BlogPosting JSON-LD with publisher and mainEntityOfPage"
      contains: "publisher"
    - path: "site/src/pages/about.md"
      provides: "Expanded about page with expertise and project links"
      contains: "casq"
  key_links:
    - from: "site/src/layouts/Layout.astro"
      to: "SITE.author and SITE.website from config.ts"
      via: "direct import (already imported)"
      pattern: "SITE\\.author|SITE\\.website"
---

<objective>
Add publisher and mainEntityOfPage fields to BlogPosting JSON-LD, and expand the about page with expertise areas and open-source project links.

Purpose: Strengthen E-E-A-T signals for Google rich results and establish author credibility.
Output: Updated Layout.astro with complete BlogPosting schema, expanded about.md with expertise and project links.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-structured-data-e-e-a-t/08-CONTEXT.md
@.planning/phases/08-structured-data-e-e-a-t/08-RESEARCH.md

<interfaces>
<!-- From site/src/layouts/Layout.astro lines 37-55: existing structuredData block -->
```typescript
const structuredData = pubDatetime
  ? {
      "@context": "https://schema.org",
      "@type": "BlogPosting",
      headline: title,
      image: `${socialImageURL}`,
      datePublished: pubDatetime.toISOString(),
      ...(modDatetime && { dateModified: modDatetime.toISOString() }),
      description: description,
      ...(tags && tags.length > 0 && { keywords: tags.join(", ") }),
      author: [
        {
          "@type": "Person",
          name: author,
          ...(profile && { url: profile }),
        },
      ],
    }
  : null;
```

<!-- From site/src/config.ts: source-of-truth values -->
```typescript
SITE.author = "Björn Roberg"
SITE.website = "https://bjro.dev/"
SITE.profile = "https://bjro.dev/"
```

<!-- canonicalURL is already in scope at Layout.astro line 28 -->
```typescript
canonicalURL = new URL(Astro.url.pathname, Astro.url)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add publisher and mainEntityOfPage to BlogPosting JSON-LD</name>
  <files>site/src/layouts/Layout.astro</files>
  <read_first>
    - site/src/layouts/Layout.astro
    - site/src/config.ts
  </read_first>
  <action>
In `site/src/layouts/Layout.astro`, add two new fields to the `structuredData` object inside the `pubDatetime` ternary, after the existing `author` array (after line 53, before the closing `}`):

```typescript
      publisher: {
        "@type": "Person",
        name: SITE.author,
        url: SITE.website,
      },
      mainEntityOfPage: {
        "@type": "WebPage",
        "@id": `${canonicalURL}`,
      },
```

Key implementation notes:
- `SITE` is already imported at line 5 (`import { SITE } from "@/config"`)
- Use `SITE.author` for publisher name (not the destructured `author` prop -- they are the same value but `SITE.author` is semantically the publisher identity)
- Use `SITE.website` for publisher URL (not `SITE.profile` -- per user decision, publisher URL is the site URL)
- Use template literal `${canonicalURL}` to coerce the URL object to string (matches the pattern at line 42 with `${socialImageURL}`)
- These fields go inside the existing `pubDatetime ? { ... } : null` ternary -- they only appear on blog post pages
- Do NOT use spread syntax -- these are unconditional fields (always present when pubDatetime is truthy)
- Use tabs for indentation (matching the existing file style)
  </action>
  <verify>
    <automated>cd /home/jani/devel/roobie.github.io/site && pnpm build 2>&1 | tail -5 && grep -c '"publisher"' /home/jani/devel/roobie.github.io/site/src/layouts/Layout.astro && grep -c '"mainEntityOfPage"' /home/jani/devel/roobie.github.io/site/src/layouts/Layout.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@type": "Person"' site/src/layouts/Layout.astro` matches at least 2 lines (author + publisher)
    - `grep 'SITE.author' site/src/layouts/Layout.astro` matches in the publisher block
    - `grep 'SITE.website' site/src/layouts/Layout.astro` matches in the publisher block
    - `grep 'mainEntityOfPage' site/src/layouts/Layout.astro` matches 1 line
    - `grep '${canonicalURL}' site/src/layouts/Layout.astro` matches in mainEntityOfPage @id
    - `cd site && pnpm build` succeeds without errors
  </acceptance_criteria>
  <done>Layout.astro emits publisher (Person, SITE.author, SITE.website) and mainEntityOfPage (WebPage, canonicalURL) in BlogPosting JSON-LD. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand about page with expertise and open-source projects</name>
  <files>site/src/pages/about.md</files>
  <read_first>
    - site/src/pages/about.md
    - site/src/layouts/AboutLayout.astro
  </read_first>
  <action>
Expand `site/src/pages/about.md` by adding two new sections between the existing intro paragraph and the CC BY 4.0 license notice. Keep the existing intro paragraph verbatim. Keep the license notice and AstroPaper attribution as the final lines.

The expanded file should have this structure:

```markdown
---
layout: ../layouts/AboutLayout.astro
title: "About"
---

I'm Björn Roberg, a software developer based in Sweden. I spend my days building
systems that have to work reliably, and my spare time wondering why they don't.
I write here to slow down my thinking — to examine ideas about technology,
epistemology, and how we reason about complex things. If any of it is useful to
you, great; if it starts a conversation, even better.

## What I work on

My focus areas are AI agents, developer tooling, and software architecture. I
build mostly in Rust, TypeScript, and Go — picking whichever fits the problem
rather than the hype cycle. Most of what I write about here comes from building
real systems and noticing patterns worth examining.

## Open-source projects

- **[casq](/posts/introducing-casq)** — content-addressable storage queue for
  deduplicating and ordering data by its hash
- **[cairn](/posts/introducing-cairn)** — append-only event store for systems
  that need an immutable audit trail
- **[slog](/posts/introducing-slog)** — structured logger designed for
  machine-readable output without sacrificing human readability

Content in this blog is licensed under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)
unless otherwise indicated.

This blog is powered by [AstroPaper](https://github.com/satnaing/astro-paper).
```

Key implementation notes:
- Match the existing casual, direct voice ("I build mostly in..." not "I am proficient in...")
- Use `##` headings for sections (rendered by AboutLayout.astro inside .app-prose)
- Link projects to their `/posts/introducing-*` blog posts (keeps traffic on-site per CONTEXT.md specifics)
- Keep descriptions terse -- one line per project
- Do NOT use HTML -- plain markdown only
- Do NOT change the frontmatter
  </action>
  <verify>
    <automated>cd /home/jani/devel/roobie.github.io/site && pnpm build 2>&1 | tail -5 && grep -c "AI agents\|developer tooling\|casq\|cairn\|slog" /home/jani/devel/roobie.github.io/site/src/pages/about.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AI agents" site/src/pages/about.md` matches
    - `grep "developer tooling" site/src/pages/about.md` matches
    - `grep "software architecture" site/src/pages/about.md` matches
    - `grep "Rust" site/src/pages/about.md` matches
    - `grep "TypeScript" site/src/pages/about.md` matches
    - `grep "/posts/introducing-casq" site/src/pages/about.md` matches
    - `grep "/posts/introducing-cairn" site/src/pages/about.md` matches
    - `grep "/posts/introducing-slog" site/src/pages/about.md` matches
    - `grep "CC BY 4.0" site/src/pages/about.md` matches (license notice preserved)
    - `grep "AstroPaper" site/src/pages/about.md` matches (attribution preserved)
    - `cd site && pnpm build` succeeds without errors
  </acceptance_criteria>
  <done>About page has expertise section (AI agents, dev tooling, software architecture, Rust/TypeScript/Go), open-source project links (casq, cairn, slog pointing to blog posts), and retains license + attribution at bottom. Build passes.</done>
</task>

</tasks>

<verification>
1. `cd site && pnpm build` — builds without errors
2. `cd site && pnpm lint` — no lint errors
3. `grep -A10 '"publisher"' site/src/layouts/Layout.astro` — shows Person type with SITE.author and SITE.website
4. `grep -A3 '"mainEntityOfPage"' site/src/layouts/Layout.astro` — shows WebPage type with canonicalURL
5. `grep -c "casq\|cairn\|slog" site/src/pages/about.md` — returns 3 (one per project)
</verification>

<success_criteria>
- BlogPosting JSON-LD includes publisher (Person, name, url) and mainEntityOfPage (WebPage, @id)
- About page lists expertise areas and links to 3 open-source project blog posts
- About page retains existing intro paragraph, CC BY 4.0 license, and AstroPaper attribution
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-structured-data-e-e-a-t/08-01-SUMMARY.md`
</output>
